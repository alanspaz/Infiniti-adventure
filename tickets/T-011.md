# T-011 — Dice / character UI surfaces

**Status:** done
**Outcome:**
- Character sheet screen: active campaign PC identity + `deriveStats`; empty-state if no campaign / empty party
- Dice screen: NdM via `rollNotation`; ability checks via `checkModifier` + `resolveCheck` (engine only)
- Wired from Home (Character sheet / Dice); theme `#140f0c` / `#d4a054`
- Empty party valid; no NSFW; no Expo device playtest
- Smoke asserts screens + App wiring + engine sheet/dice path; docs/zip refreshed
