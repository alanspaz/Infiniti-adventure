# Q-01 — Quests

**Status:** done
**Outcome:**
- `world.quests[]` on CampaignState: id, title, summary?, progressNotes?, status active|done|failed
- Patches: acceptQuest / updateQuest / completeQuestId / failQuestId (+ upsertQuest stub)
- New campaigns seed starter quest `quest-first-steps`; empty journal never crashes
- Quest tab: Active + Done (+ Failed); reads CampaignState context only
- Story `patchesFromSceneBeat` accept/update/complete hooks; tests + smoke green
