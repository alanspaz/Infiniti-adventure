# T-014 — Remote narrator settings polish

**Status:** done
**Outcome:**
- Settings UI: provider kind + API key + **base URL** + optional **model**; clear error when remote selected but missing key/URL
- Prefs blob persists `baseUrl` / `model` with verbosity + providerKind; API key stays in SecureStore (never logged)
- `createPlayNarrator` wires remote with base URL + key + `enableHttp` + real/mock fetch; stub fallback when offline/misconfigured; on-device reserved message
- contracts/narrator.md + docs + smoke/unit tests (mocked fetch); flat zip refresh; GitHub push when green
- No Expo device playtest (held); no NSFW; empty party valid
