# T-015 — Settings storage

**Status:** accepted / source
**Outcome:** SecureStore keys ia.settings.apiKey, ia.settings.prefs; in-memory Map fallback; never log API key; verbosity + ProviderKind. Settings reachable from Home and from PlayShell Settings tab.
