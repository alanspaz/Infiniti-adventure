# T-012 — Scene / adventure loop

**Status:** done
**Outcome:**
- `contracts/scene.md` + narrator contract updates (`checkHint`, optional remote HTTP)
- Pure TS `product/engine/scene.ts`: detect check/travel, `resolveSceneBeat` / `resolveOpeningBeat`
- SceneScreen: load campaign, stub narrator (remote optional + stub fallback), action input, dice heuristic, travel, show-me still stub, persist via `onCampaignChange`
- Home Continue / New scene → Scene; verbosity from settings
- Empty party valid; no NSFW; no Expo device playtest
- Unit tests + smoke + docs + flat zip refresh
