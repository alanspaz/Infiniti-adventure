# T-018 — Map travel ↔ Story sync

**Status:** done
**Outcome:** SceneScreen watches `session.locationId`; Map-driven travel updates Story place line and fires a `continue` beat with `skipTravel` so Story stays in sync without double-travel.
