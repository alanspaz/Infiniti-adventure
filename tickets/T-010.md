# T-010 — PersistStore AsyncStorage adapter (app shell)

**Status:** done
**Outcome:**
- AsyncPersistStore via `@react-native-async-storage/async-storage` + memory fallback (settings-style dynamic import)
- MemoryPersistStore kept for engine tests
- `ACTIVE_CAMPAIGN_KEY` (`ia.save.activeId`); `persistCampaign` / `loadActiveCampaign`
- Wired Identity create, Home continue (restore + Continue → map), Map travel saves
- contracts/save.md updated; unit tests + smoke; no device Expo playtest
