# Backlog

## Next
- Remote narrator base URL in settings + polish (HTTP optional when configured)
- Companion emotion / party UX (only if party features are prioritized)
- Device Expo playtest (held)

## Notes
T-013 Stills image UI / cache persistence is done — offline placeholders persist on device. See board/DONE.md.
Suggested pause point: core playable offline; device Expo playtest can resume later.
